{
  "name": "kanban-task-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Environment-independent task synchronization and automation engine",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.0.0"
  }
}
