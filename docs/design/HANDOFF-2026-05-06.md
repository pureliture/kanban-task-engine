# Handoff: GitHub README SVG Rendering Hardening — Visual Asset Slice

**Date:** 2026-05-06
**Status:** Local repo verification complete; GitHub-hosted README verification pending push
**Scope:** README embed asset, one-page SVG hardening, draw.io/HTML label parity, verifier, CI docs gate, design docs

---

## 변경 파일

| 파일 | 상태 | 설명 |
|---|---|---|
| `README.md` | 수정 | architecture embed를 compact SVG로 교체하고 full-size 링크는 one-page SVG로 유지 |
| `docs/design/kanban-task-engine-one-page.svg` | 수정 | `<title>`, `<desc>`, 내부 `<defs><style>`, full-viewBox 배경 추가; 존재하지 않는 Codex adapter 라벨과 old transition constant 라벨 수정 |
| `docs/design/kanban-task-engine-one-page.drawio` | 수정 | SVG와 같은 의미로 `codex-runner`, `IssueStatus (6)`, `VALID_ISSUE_TRANSITIONS (8)` 라벨 정합성 보강 |
| `docs/design/kanban-task-engine-architecture-overview.svg` | 신규 | GitHub README embed용 720x540 self-contained compact SVG |
| `docs/design/README.md` | 수정 | GitHub README SVG Contract, maintenance guard, `python3 scripts/verify-docs.py` 명령 추가 |
| `docs/design/kanban-task-engine-one-page.html` | 수정 | interactive view의 Codex executor와 status transition label을 SVG/drawio와 동기화 |
| `docs/design/HANDOFF-2026-05-06.md` | 신규 | 이 visual asset/docs slice 인계 문서 |
| `scripts/verify-docs.py` | 수정 | `--root`, XML/SVG contract, path containment, draw.io XML parse, runtime truth checks 추가 |
| `scripts/verify-docs.test.ts` | 신규 | fixture 기반 regression tests |
| `package.json`, `.github/workflows/ci.yml` | 수정 | `pnpm test:docs` script와 CI docs gate 추가 |
| `scripts/eval-superpowers.ts`, `scripts/check-hardening.ts` | 수정 | docs verification CI/eval guard 추가 |

---

## 핵심 결정

- README에는 full one-page SVG를 직접 embed하지 않고 `kanban-task-engine-architecture-overview.svg`를 사용합니다.
- README image의 `<a href>`는 계속 `docs/design/kanban-task-engine-one-page.svg`를 가리켜 full-size 상세 다이어그램 접근을 유지합니다.
- `kanban-task-engine-one-page.svg`는 raw SVG로 열어도 깨지지 않도록 self-contained style/background/accessibility metadata를 포함합니다.
- Codex 실행은 non-existent adapter package가 아니라 `codex-runner` 의미로 표기합니다.
- status contract 표기는 `IssueStatus (6)`와 `VALID_ISSUE_TRANSITIONS (8)`로 통일했습니다.

---

## Verification

2026-05-06 local verification:

```bash
pnpm -r build
pnpm -r test
pnpm test:docs
pnpm eval:superpowers
pnpm eval:hardening -- --strict-architecture
git diff --check HEAD
```

All commands passed locally.

Visual QA:

- `qlmanage -t -s 900 -o /private/tmp docs/design/kanban-task-engine-architecture-overview.svg`
- `qlmanage -t -s 1600 -o /private/tmp docs/design/kanban-task-engine-one-page.svg`

Both SVGs produced PNG thumbnails under `/private/tmp/`.

## 남은 운영 확인

- GitHub-hosted README 렌더링은 push 이후 github.com에서 별도 확인이 필요합니다. 현재 상태는 `local_repo_ready: true`, `github_hosted_verified: false`입니다.
- draw.io에서 재export하면 내부 SVG hardening layer가 사라질 수 있으므로 export 후 `docs/design/README.md`의 contract를 다시 적용해야 합니다.
