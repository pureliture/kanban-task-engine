{
  "name": "@kanban-task-engine/adapter-openclaw",
  "version": "0.1.0",
  "description": "OpenClaw Gateway adapter for task execution",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@kanban-task-engine/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.0.0"
  }
}