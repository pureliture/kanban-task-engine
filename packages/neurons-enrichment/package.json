{
  "name": "@kanban-task-engine/neurons-enrichment",
  "version": "0.1.0",
  "description": "Read-only neurons knowledge enrichment provider for kanban board projection (Phase 1, D3 A노선)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@kanban-task-engine/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.0.0"
  }
}
